{
  "name": "desafio-serverest-bdd-front",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run:ui": "cypress run --spec 'cypress/e2e/ui/**/*.feature'",
    "cy:run:api": "cypress run --spec 'cypress/e2e/api/**/*.feature'",
    "cy:run:all": "cypress run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^20.1.1",
    "@cypress/webpack-preprocessor": "^6.0.2",
    "cypress": "^13.13.0",
    "esbuild-wasm": "^0.28.1"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/e2e/**/*.js"
  }
}